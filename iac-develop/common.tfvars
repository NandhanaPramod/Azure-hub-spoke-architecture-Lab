location = "uaenorth"

default_tags = {
  ProjectName  = "ISKAN-ADHA",
  BusinessUnit = "IT",
}


# ## Common `basename` was moved as default for each module's default variables.
# ## Reason: renaming is in progress and pre-prod is being created.
# ## Please don't uncomment!
# basename = "oss"
